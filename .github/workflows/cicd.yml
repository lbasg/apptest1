on: 
  push:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  CICD:
    uses: lbasg/cicd/.github/workflows/main.yml@main
    with: 
      application: ${{ vars.APP_NAME }}
      technology: ${{ vars.APP_TECH }}
